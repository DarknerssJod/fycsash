class Person:
    def __int__(self_name,surname,birthdate,address,telephone,email):
        self_name=name
        with open(fname) as f:
            if bufsize>fsize:
                bufsize=fsize-1
                print(bufsize)
                data=[]
                while True:
                    iter+=1
                    f.seek(iter)
                    data.extend(f.readline())
                    
